{
  "name": "community-circles-monorepo",
  "private": true,
  "workspaces": [
    "apps/web",
    "services/auth",
    "services/gateway"
  ],
  "scripts": {
    "dev:web": "npm --workspace apps/web run dev",
    "dev:auth": "npm --workspace services/auth run dev",
    "dev:gateway": "npm --workspace services/gateway run dev",
    "test": "npm --workspaces run test"
  }
}
